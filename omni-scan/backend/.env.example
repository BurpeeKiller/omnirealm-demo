# =========================================
# OMNISCAN BACKEND - VARIABLES D'ENVIRONNEMENT
# =========================================
# Copiez ce fichier vers .env et ajustez les valeurs

# =========================================
# APPLICATION
# =========================================
ENVIRONMENT=development
SECRET_KEY=your-secret-key-here-generate-with-openssl-rand-hex-32
DEBUG=true

# Backend URL (pour CORS et redirections)
BACKEND_URL=http://localhost:8000

# =========================================
# SUPABASE (BASE DE DONNÉES ET AUTH)
# =========================================
# URL de votre instance Supabase
SUPABASE_URL=https://your-project.supabase.co

# Clé anonyme (publique) - safe pour le client
SUPABASE_ANON_KEY=your-anon-key-here

# Clé de service (privée) - JAMAIS côté client
SUPABASE_SERVICE_KEY=your-service-key-here

# =========================================
# IA ET OCR
# =========================================
# Clé API OpenAI pour l'analyse IA
OPENAI_API_KEY=sk-your-openai-key-here

# Modèle OpenAI à utiliser
OPENAI_MODEL=gpt-4o-mini

# Langues pour l'OCR Tesseract (fra+eng par défaut)
OCR_LANGUAGES=fra+eng

# =========================================
# UPLOADS ET FICHIERS
# =========================================
# Taille maximum des fichiers en MB
MAX_FILE_SIZE_MB=10

# Dossier pour stocker les uploads
UPLOAD_PATH=./uploads

# Dossier temporaire pour le traitement
TEMP_PATH=./temp

# =========================================
# SÉCURITÉ ET LIMITES
# =========================================
# Limite de requêtes par minute par IP
RATE_LIMIT_PER_MINUTE=60

# CORS : URLs autorisées (séparées par des virgules)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174

# =========================================
# LOGGING ET MONITORING
# =========================================
# Niveau de log (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Format des logs (json pour production, simple pour dev)
LOG_FORMAT=json

# =========================================
# PRODUCTION UNIQUEMENT
# =========================================
# Utilisez des variables d'environnement système en production
# Jamais de fichier .env en production !

# Exemple de configuration production via variables système:
# export ENVIRONMENT=production
# export SECRET_KEY=$(openssl rand -hex 32)
# export SUPABASE_URL=https://your-prod-instance.supabase.co
# etc...